What I Did
Checked that the VMSS had the desired number of instances and verified that all VMs were running and healthy. Took a screenshot showing the scaling configuration.
How Scaling Works
Manual scaling lets you set the number of VMs. Auto-scaling adjusts the number of VMs based on metrics like CPU usage. Scale out adds VMs when load increases, and scale in removes VMs when load decreases. This ensures the application stays available and efficient.
